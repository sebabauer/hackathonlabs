<%= form_with(model: idea_like, local: true) do |form| %>
  <% if idea_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idea_like.errors.count, "error") %> prohibited this idea_like from being saved:</h2>

      <ul>
      <% idea_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idea_id %>
    <%= form.text_field :idea_id, id: :idea_like_idea_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :idea_like_user_id %>
  </div>

  <div class="field">
    <%= form.label :liked %>
    <%= form.check_box :liked, id: :idea_like_liked %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
